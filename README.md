# Luca Silveira Personal Site

Minimal personal archive built with Astro and Tailwind CSS.

## Commands

```sh
npm install
npm run dev
npm run build
```

The site is ready for Cloudflare Pages or GitHub Pages. Build command: `npm run build`. Output directory: `dist`.
